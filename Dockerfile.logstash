FROM docker.elastic.co/logstash/logstash:8.7.1

# Install Logstash plugins required
RUN logstash-plugin install logstash-filter-xml

# You can add more plugin installations here if needed
# RUN logstash-plugin install <another-plugin>
